[package]
name = "rox"
version = "0.1.0"
edition = "2024"

[dev-dependencies]
criterion ={ version = "0.5", features = ["html_reports"] }

[[bench]]
name = "rox_benches"
harness = false

[dependencies]
string-interner = "=0.19.0"
rlox_gc_derive =  { path = "../rlox_gc_derive" }